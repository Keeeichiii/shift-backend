<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="create_table_transactions" author="ea.novak">

        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="true">
                SELECT (
                           CASE
                               WHEN NOT EXISTS (
                                   SELECT 1
                                   FROM information_schema.tables
                                   WHERE table_schema = 'public'
                                     AND table_name = 'transactions'
                               )
                                   THEN 'true'
                               ELSE 'false'
                               END
                           );
            </sqlCheck>
        </preConditions>
        <comment>Создание таблицы транзакций</comment>

        <createTable tableName="transactions">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="bigint">
                <constraints nullable="false" referencedTableName="users" referencedColumnNames="id" foreignKeyName="fk_transactions_user" deleteCascade="true"/>
            </column>
            <column name="transaction_date" type="timestamp with time zone" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="total_amount" type="decimal(10,2)">
                <constraints nullable="false"/>
            </column>
            <column name="status" type="transaction_status" defaultValue="PENDING">
                <constraints nullable="false"/>
            </column>
            <column name="kind" type="transaction_kind">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="varchar(255)"/>
        </createTable>
        <sql dbms="postgresql"><![CDATA[
ALTER TABLE transactions ADD CONSTRAINT chk_transaction_total_amount_non_negative CHECK (total_amount >= 0);
]]></sql>
        <createIndex indexName="idx_transactions_user_date" tableName="transactions">
            <column name="user_id"/>
            <column name="transaction_date"/>
        </createIndex>

        <rollback>
            <dropTable tableName="transactions"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
